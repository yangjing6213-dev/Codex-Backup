# ENHE Codex Backup has no required runtime environment variables.
# Never put tokens, passwords, cookies, private keys, or personal paths here.
# Local acceptance-only variables, when explicitly needed, must be supplied
# in the test process and must never be committed with real values.
