{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "ENHE Codex Backup main window",
  "windows": ["main"],
  "permissions": [
    "core:app:allow-version",
    "process:allow-restart"
  ]
}
